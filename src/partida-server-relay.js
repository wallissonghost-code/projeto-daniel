(()=>{'use strict';
const RELAY_KEY='liveplus-game-relay-endpoint',AUTH_KEY='liveplus-game-relay-key',LEGACY_RELAY_KEY='liveplus-relay-endpoint',MODE_KEY='liveplus-game-transport-mode';
const state={code:'',ready:false,game:false,socket:'offline',authenticated:false,endpoint:'',mode:'hybrid',failures:0,nextRetryAt:0};let ws=null,connectPromise=null,reconnectTimer=null,manualClose=false;
const log=(text,tone='')=>{const out=document.getElementById('matchLog');if(!out)return;const row=document.createElement('div');row.className='matchLogItem '+tone;row.textContent=new Date().toLocaleTimeString('pt-BR')+' · '+text;out.prepend(row);while(out.children.length>12)out.lastChild.remove()};
const currentCode=()=>String(window.LivePlusMatch?.getCode?.()||'').trim().toUpperCase();
function normalizeEndpoint(raw){try{const u=new URL(String(raw||''));if(!['ws:','wss:'].includes(u.protocol))return'';if(location.protocol==='https:'&&u.protocol==='ws:')u.protocol='wss:';return u.toString()}catch{return''}}
function mode(){try{return localStorage.getItem(MODE_KEY)==='peer'?'peer':'hybrid'}catch{return'hybrid'}}
function relayKey(){try{return String(localStorage.getItem(AUTH_KEY)||'')}catch{return''}}
function configuredEndpoint(){try{const q=new URLSearchParams(location.search).get('gameRelay')||new URLSearchParams(location.search).get('liveplusRelay');const chosen=normalizeEndpoint(q||localStorage.getItem(RELAY_KEY)||window.LIVEPLUS_GAME_RELAY_ENDPOINT||'');if(chosen){localStorage.setItem(RELAY_KEY,chosen);return chosen}return normalizeEndpoint(localStorage.getItem(LEGACY_RELAY_KEY)||'')}catch{return normalizeEndpoint(window.LIVEPLUS_GAME_RELAY_ENDPOINT||'')}}
function configure(endpoint,key){const value=normalizeEndpoint(endpoint);if(!value)return false;try{localStorage.setItem(RELAY_KEY,value);if(key!==undefined)localStorage.setItem(AUTH_KEY,String(key||''))}catch{}state.endpoint=value;syncUI();return true}
function clearEndpoint(){try{localStorage.removeItem(RELAY_KEY);localStorage.removeItem(AUTH_KEY)}catch{}state.endpoint='';closeSocket(true);syncUI()}
function setMode(value){state.mode=value==='peer'?'peer':'hybrid';try{localStorage.setItem(MODE_KEY,state.mode)}catch{}if(state.mode==='peer')closeSocket(true);else if(currentCode()&&configuredEndpoint())ensureSocket().catch(()=>{});syncUI();return state.mode}
function refreshTransport(){const el=document.getElementById('matchTransport');if(!el)return;if(state.game)el.textContent='SERVIDOR ONLINE';else if(state.mode==='peer')el.textContent='WEBRTC ONLINE';else if(!state.endpoint)el.textContent='WEBRTC ONLINE';else if(state.ready)el.textContent='SERVIDOR AGUARDANDO';else if(state.socket==='connecting'||state.socket==='authenticating')el.textContent='SERVIDOR CONECTANDO';else if(state.socket==='auth-error')el.textContent='WEBRTC · CHAVE INVÁLIDA';else el.textContent='WEBRTC FALLBACK'}
function syncUI(){state.mode=mode();state.endpoint=configuredEndpoint();const input=document.getElementById('gameRelayEndpoint'),key=document.getElementById('gameRelayKey'),field=document.getElementById('gameRelayFields'),primary=document.getElementById('matchRelayPrimary'),backup=document.getElementById('matchRelayBackup');if(input&&document.activeElement!==input)input.value=state.endpoint;if(key&&document.activeElement!==key)key.value=relayKey();if(field)field.hidden=state.mode==='peer';document.querySelectorAll('[data-match-transport]').forEach(b=>b.classList.toggle('active',b.dataset.matchTransport===state.mode));if(primary)primary.textContent=state.mode==='peer'?'PRINCIPAL · WEBRTC':state.game?'PRINCIPAL · SERVIDOR ONLINE':!state.endpoint?'PRINCIPAL · WEBRTC':state.ready?'PRINCIPAL · SERVIDOR PRONTO':'PRINCIPAL · WEBRTC FALLBACK';if(backup)backup.textContent=state.mode==='peer'?'SEM SERVIDOR · CONEXÃO ANTIGA':state.endpoint?'BACKUP · WEBRTC PRONTO':'SERVIDOR OPCIONAL · NÃO CONFIGURADO';refreshTransport()}
function send(payload){if(ws?.readyState!==WebSocket.OPEN||!state.authenticated)return false;try{ws.send(JSON.stringify(payload));return true}catch{return false}}
function registerRoom(){const code=currentCode()||state.code;if(!code||!state.authenticated||state.mode==='peer')return false;state.code=code;return send({type:'relay_panel_create',code,ttlMs:5*60*1000})}
function handleGamePayload(data){if(!data||typeof data!=='object')return;if(data.type==='game_manifest'){window.dispatchEvent(new CustomEvent('liveplus-game-manifest',{detail:data.manifest||data}));return}if(data.type==='state'){window.dispatchEvent(new CustomEvent('liveplus-game-state',{detail:data}));return}if(data.type==='event'){window.dispatchEvent(new CustomEvent('liveplus-game-event',{detail:data}));return}window.dispatchEvent(new CustomEvent('liveplus-game-message',{detail:data}))}
function handleMessage(m){if(!m||typeof m!=='object')return;if(m.type==='bridge'){if(m.authRequired){state.socket='authenticating';try{ws?.send(JSON.stringify({type:'auth',key:relayKey()}))}catch{};syncUI()}else{state.authenticated=true;state.socket='online';state.failures=0;registerRoom()}return}if(m.type==='auth'){state.authenticated=!!m.ok;if(!m.ok){state.socket='auth-error';state.failures++;state.nextRetryAt=Date.now()+30000;log('Chave privada do relay recusada · WebRTC assumiu.','warn');try{ws?.close()}catch{};syncUI();return}state.socket='online';state.failures=0;state.nextRetryAt=0;registerRoom();return}if(m.type==='relay_panel_ready'&&(!state.code||m.code===state.code)){state.code=m.code;state.ready=true;state.game=!!m.gameConnected;log('Relay do servidor pronto · '+m.code,'ok');syncUI();return}if(m.type==='relay_game_connected'&&m.code===state.code){state.ready=true;state.game=true;log('Jogo conectado pelo servidor.','ok');syncUI();return}if(m.type==='relay_game_disconnected'&&m.code===state.code){state.game=false;log('Servidor perdeu o jogo · WebRTC permanece disponível.','warn');syncUI();return}if(m.type==='relay_message'&&m.from==='game'&&m.code===state.code){state.game=true;handleGamePayload(m.payload);syncUI();return}if(m.type==='relay_error'&&m.scope==='auth'){state.socket='auth-error';state.nextRetryAt=Date.now()+30000;log('Autenticação do relay falhou · WebRTC assumiu.','warn');syncUI()}}
function closeSocket(stopReconnect=false){if(stopReconnect)manualClose=true;clearTimeout(reconnectTimer);reconnectTimer=null;state.ready=false;state.game=false;state.authenticated=false;if(state.socket!=='auth-error')state.socket='offline';try{ws?.close()}catch{}ws=null;connectPromise=null}
function scheduleReconnect(){if(manualClose||reconnectTimer||!currentCode()||mode()==='peer'||!configuredEndpoint())return;const delay=Math.max(10000,Math.min(60000,10000*Math.max(1,state.failures)),state.nextRetryAt-Date.now());reconnectTimer=setTimeout(()=>{reconnectTimer=null;ensureSocket().catch(()=>{})},delay)}
function ensureSocket(){state.mode=mode();const target=configuredEndpoint();state.endpoint=target;if(state.mode==='peer'||!target||!currentCode()){syncUI();return Promise.resolve(false)}if(Date.now()<state.nextRetryAt){syncUI();scheduleReconnect();return Promise.resolve(false)}if(ws?.readyState===WebSocket.OPEN)return Promise.resolve(true);if(connectPromise)return connectPromise;manualClose=false;state.socket='connecting';syncUI();connectPromise=new Promise(resolve=>{let done=false;const finish=v=>{if(!done){done=true;resolve(v)}};try{ws=new WebSocket(target)}catch{state.socket='offline';state.failures++;finish(false);return}const timeout=setTimeout(()=>{if(!state.authenticated){state.socket='offline';state.failures++;state.nextRetryAt=Date.now()+10000;try{ws?.close()}catch{};finish(false)}},6000);ws.onmessage=ev=>{let m;try{m=JSON.parse(ev.data)}catch{return}handleMessage(m);if(state.authenticated){clearTimeout(timeout);finish(true)}};ws.onerror=()=>finish(false);ws.onclose=()=>{clearTimeout(timeout);const was=state.game;state.authenticated=false;state.ready=false;state.game=false;ws=null;connectPromise=null;if(state.socket!=='auth-error')state.socket='offline';if(was)log('Relay caiu · WebRTC assumiu. Nova tentativa será feita em segundo plano.','warn');syncUI();scheduleReconnect();finish(false)}}).finally(()=>{connectPromise=null});return connectPromise}
async function create(){state.mode=mode();state.code=currentCode();if(!state.code||state.mode==='peer'||!configuredEndpoint()){syncUI();return false}return ensureSocket()}
function leave(){if(state.code&&state.authenticated)send({type:'relay_leave',code:state.code});state.code='';closeSocket(true);syncUI()}
function resumeRelay(){if(!currentCode()||mode()==='peer'||!configuredEndpoint())return;manualClose=false;ensureSocket().catch(()=>{})}
window.addEventListener('load',()=>{state.mode=mode();syncUI();const endpoint=document.getElementById('gameRelayEndpoint'),key=document.getElementById('gameRelayKey');const save=()=>{const raw=endpoint?.value.trim()||'';if(!raw){clearEndpoint();log('Relay removido · usando somente WebRTC.','ok');return}if(!configure(raw,key?.value||'')){log('Endpoint inválido. Use ws:// ou wss://.','error');return}state.failures=0;state.nextRetryAt=0;closeSocket(true);manualClose=false;log('Configuração do relay salva.','ok');if(currentCode()&&mode()==='hybrid')ensureSocket().catch(()=>{})};endpoint?.addEventListener('change',save);key?.addEventListener('change',save);document.querySelectorAll('[data-match-transport]').forEach(btn=>btn.addEventListener('click',()=>{const next=setMode(btn.dataset.matchTransport);log(next==='peer'?'Somente WebRTC ativado.':'Servidor + WebRTC ativado.','ok')}));document.getElementById('newMatchCode')?.addEventListener('click',()=>setTimeout(create,0));document.getElementById('endMatchSession')?.addEventListener('click',leave);const match=window.LivePlusMatch;if(!match||match.__serverRelayWrapped)return;const peerSend=match.send.bind(match),peerEnd=match.end.bind(match);match.send=data=>state.mode==='hybrid'&&state.game&&state.code&&send({type:'relay_panel_message',code:state.code,payload:data})||peerSend(data);match.end=()=>{leave();return peerEnd()};match.getTransport=()=>state.game?'server':'peer';match.__serverRelayWrapped=true});
document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='visible')resumeRelay()});window.addEventListener('pageshow',resumeRelay);window.addEventListener('online',resumeRelay);
window.LivePlusServerRelay={create,leave,resume:resumeRelay,configure,clearEndpoint,setMode,state:()=>({...state,mode:mode()}),endpoint:configuredEndpoint};
})();