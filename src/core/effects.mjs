export function pushTimedFx(list,fx,{max=96}={}){list.push(fx);if(list.length>max)list.splice(0,list.length-max);return fx}
export function pruneTimedFx(list,now){return list.filter(f=>(f.until??f.at??0)>now)}
export function coalesceDamageFx(list,{entity,amount,kind='normal',now,x,y,max=48,windowMs=115}){if(!entity||!Number.isFinite(amount)||amount<=0)return list;const last=list.length?list[list.length-1]:null;if(last&&last.e===entity&&now-last.at<windowMs&&last.kind===kind){last.amount+=amount;last.at=now;return list}list.push({e:entity,amount,kind,at:now,x:x??entity.x,y:y??entity.y});if(list.length>max)list.splice(0,list.length-max);return list}
export function assertEffectsDomain(){const e={x:1,y:2},a=[];coalesceDamageFx(a,{entity:e,amount:2,now:100});coalesceDamageFx(a,{entity:e,amount:3,now:150});if(a.length!==1||a[0].amount!==5)throw Error('damage fx coalescing invalid');pushTimedFx(a,{until:1},{max:1});if(a.length!==1)throw Error('fx budget invalid');return true}
