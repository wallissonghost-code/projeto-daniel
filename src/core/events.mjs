export const METEOR_DEFAULTS=Object.freeze({interval:1.7,warning:1.8,radius:92,playerDamage:18,mobDamage:20,batch:1});
export const METEOR_LIMITS=Object.freeze({interval:[.45,12],warning:[.6,5],radius:[45,180],playerDamage:[1,80],mobDamage:[1,100],batch:[1,25]});
export function clampEventNumber(v,min,max,fallback){v=Number(v);return Number.isFinite(v)?Math.max(min,Math.min(max,v)):fallback}
export function normalizeMeteorConfig(input={},base=METEOR_DEFAULTS){const out={...base};for(const k of ['interval','warning','radius','playerDamage','mobDamage']){const [min,max]=METEOR_LIMITS[k];out[k]=clampEventNumber(input[k],min,max,out[k])}const [bmin,bmax]=METEOR_LIMITS.batch;out.batch=Math.round(clampEventNumber(input.batch,bmin,bmax,out.batch));return out}
export function createMeteorSchedule({player,W,H,config=METEOR_DEFAULTS,random=Math.random}){const halfW=Math.max(170,W*.43),halfH=Math.max(170,H*.43),near=random()<.28,spread=near?110:1,x=near?player.x+(random()*2-1)*spread:player.x+(random()*2-1)*halfW,y=near?player.y+(random()*2-1)*spread:player.y+(random()*2-1)*halfH;return{x,y,r:config.radius,warningLeft:config.warning,warningTotal:config.warning,hit:false,life:0}}
export function meteorKiller(m){return{eventName:'CHUVA DE METEORO',type:null,tier:0,x:m.x,y:m.y,r:m.r,dead:false}}
export function assertEventDomain(){const c=normalizeMeteorConfig({interval:99,batch:99,radius:1});if(c.interval!==12||c.batch!==25||c.radius!==45)throw Error('event config clamp invalid');const m=createMeteorSchedule({player:{x:0,y:0},W:500,H:500,random:()=>.5});if(!Number.isFinite(m.x)||m.r!==92)throw Error('meteor schedule invalid');return true}
