import WebSocket from 'ws';
import {safeSend} from './protocol.mjs';
const cleanCode=v=>String(v||'').trim().toUpperCase(),alive=ws=>ws&&ws.readyState===WebSocket.OPEN;
function relayUrl(base,code){try{const u=new URL(base);if(!u.pathname||u.pathname==='/')u.pathname='/relay';if(!u.searchParams.has('code'))u.searchParams.set('code',code);return u.toString()}catch{return base}}
export class ServerAutomation{
  constructor(clientWs){this.clientWs=clientWs;this.ws=null;this.endpoint='';this.key='';this.code='';this.ready=false;this.game=false;this.seq=0;this.desired=false;this.reconnectTimer=null;this.heartbeatTimer=null;this.failures=0}
  setClientWs(ws){this.clientWs=ws||null;return this}
  notify(extra={}){safeSend(this.clientWs,{type:'server_automation_status',ok:true,provider:'cloudflare',code:this.code,ready:this.ready,game:this.game,...extra})}
  configure({endpoint='',key='',code=''}={}){const nextEndpoint=String(endpoint||'').trim(),nextCode=cleanCode(code);if(!/^wss?:\/\//i.test(nextEndpoint)||!nextCode)return false;const reconnect=nextEndpoint!==this.endpoint||nextCode!==this.code||String(key||'')!==this.key;this.endpoint=nextEndpoint;this.key=String(key||'');this.code=nextCode;this.desired=true;if(reconnect||!alive(this.ws))this.connect();else this.register();return true}
  update(){this.notify({desired:this.desired});return true}
  clearTimers(){if(this.reconnectTimer){clearTimeout(this.reconnectTimer);this.reconnectTimer=null}if(this.heartbeatTimer){clearInterval(this.heartbeatTimer);this.heartbeatTimer=null}}
  closeSocket(){const ws=this.ws;this.ws=null;try{ws?.removeAllListeners?.()}catch{}try{ws?.close()}catch{}this.ready=false;this.game=false}
  scheduleReconnect(reason='relay_closed'){if(!this.desired||!this.endpoint||!this.code||this.reconnectTimer)return;const delay=Math.min(15000,1000*Math.max(1,2**Math.min(this.failures,4)));this.notify({ok:false,error:reason,reconnecting:true,delay});this.reconnectTimer=setTimeout(()=>{this.reconnectTimer=null;if(this.desired)this.connect()},delay);this.reconnectTimer.unref?.()}
  startHeartbeat(){if(this.heartbeatTimer)clearInterval(this.heartbeatTimer);this.heartbeatTimer=setInterval(()=>{if(!this.desired)return;if(!alive(this.ws)){this.scheduleReconnect('relay_offline');return}try{this.ws.send(JSON.stringify({type:'ping',at:Date.now()}))}catch{this.scheduleReconnect('relay_ping_failed')}},20000);this.heartbeatTimer.unref?.()}
  connect(){if(!this.desired||!this.endpoint||!this.code)return false;if(this.reconnectTimer){clearTimeout(this.reconnectTimer);this.reconnectTimer=null}this.closeSocket();let ws;try{ws=new WebSocket(relayUrl(this.endpoint,this.code))}catch{this.failures++;this.scheduleReconnect('relay_open_failed');return false}this.ws=ws;ws.on('open',()=>{if(this.ws!==ws)return;this.failures=0;this.startHeartbeat()});ws.on('message',raw=>{if(this.ws!==ws)return;let m;try{m=JSON.parse(raw.toString())}catch{return}if(m.type==='bridge'){if(m.authRequired)ws.send(JSON.stringify({type:'auth',key:this.key}));else this.register()}else if(m.type==='auth'){if(m.ok)this.register();else{this.notify({ok:false,error:'relay_auth'});this.desired=false}}else if(m.type==='relay_ingress_ready'){this.ready=true;this.game=!!m.gameConnected;this.failures=0;this.notify({automation:m.automation||'',persistent:true})}else if(m.type==='relay_game_connected'){this.game=true;this.notify({persistent:true})}else if(m.type==='relay_game_disconnected'){this.game=false;this.notify({persistent:true})}else if(m.type==='automation_event_ack'){safeSend(this.clientWs,{type:'server_automation_event_ack',code:this.code,...m})}else if(m.type==='relay_error')this.notify({ok:false,error:m.message||m.scope||'relay_error'})});ws.on('close',()=>{if(this.ws!==ws)return;this.ws=null;this.ready=false;this.game=false;this.failures++;this.scheduleReconnect('relay_closed')});ws.on('error',()=>{if(this.ws!==ws)return;this.failures++;this.scheduleReconnect('relay_error')});return true}
  register(){if(!alive(this.ws)||!this.code)return false;try{this.ws.send(JSON.stringify({type:'relay_ingress_join',code:this.code,protocol:'liveplus-cloud-automation-v1'}));return true}catch{this.scheduleReconnect('relay_register_failed');return false}}
  onTikTok(event){if(!event||!['gift','like','chat','follow','share'].includes(event.type))return 0;if(!this.ready||!alive(this.ws)){this.scheduleReconnect('relay_not_ready');return 0}const now=Date.now(),eventId=String(event.eventId||event.msgId||event.messageId||`${this.code}:${now}:${++this.seq}`),payload={...event,eventId,receivedAt:now};try{this.ws.send(JSON.stringify({type:'relay_ingress_event',code:this.code,eventId,receivedAt:now,event:payload}));return 1}catch{this.scheduleReconnect('relay_send_failed');return 0}}
  close(){this.desired=false;this.clearTimers();this.closeSocket()}
}
